# Used by `supabase functions serve --env-file .env` and `supabase secrets set --env-file .env`.
ELASTICEMAIL_API_KEY=your_api_key
EMAIL_FROM=Acme <hello@yourdomain.com>
EMAIL_TO=you@yourdomain.com
ELASTICEMAIL_WEBHOOK_TOKEN=change_me

# Optional: Supabase Auth hook secret (format v1,whsec_<base64>). Leave empty to skip verification locally.
SEND_EMAIL_HOOK_SECRET=
