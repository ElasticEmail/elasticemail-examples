# Copy to .secret.local for the emulator only. In production these come from
# `firebase functions:secrets:set <NAME>` (Cloud Secret Manager).
ELASTICEMAIL_API_KEY=your_api_key
ELASTICEMAIL_WEBHOOK_TOKEN=change_me
