# Local development only. On Cloud Run the API key and webhook token come from Secret Manager
# (--set-secrets) and EMAIL_FROM from --set-env-vars.
ELASTICEMAIL_API_KEY=your_api_key
EMAIL_FROM=Acme <hello@yourdomain.com>
EMAIL_TO=you@yourdomain.com
ELASTICEMAIL_WEBHOOK_TOKEN=change_me
PORT=8080
