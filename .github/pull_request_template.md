## What changed

## Stacks touched

## How it was tested

- [ ] Stack checks pass (see AGENTS.md, "Checking your change")
- [ ] Sent a real email / received a real webhook (say which), or compiled only
- [ ] Both `typescript/` and `javascript/` variants updated, where the stack has both
- [ ] Ran `node scripts/build-agent-files.mjs` if example files were added, renamed or removed
